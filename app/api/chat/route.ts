import { NextResponse } from 'next/server';
import OpenAI from 'openai';

// Typhoon AI Client (OpenAI-compatible)
const typhoon = new OpenAI({
  apiKey: process.env.TYPHOON_API_KEY || '',
  baseURL: 'https://api.opentyphoon.ai/v1',
});

export async function POST(req: Request) {
  try {
    const { message, history } = await req.json();

    // แปลง history format
    const messages: OpenAI.ChatCompletionMessageParam[] = [
      {
        role: 'system',
        content: `คุณคือ "น้องเรียนดี" เพื่อนสนิทวัยเรียนที่พร้อมคุยทุกเรื่อง จาก Mind Buddy 💖

🎯 บุคลิก:
- พูดเหมือนเพื่อนสนิท ใช้ "เรา/แก" หรือ "เรา/เธอ"
- ภาษาวัยรุ่นไทยเป็นกันเอง (โห, อือ, จริงแก, เห้อออ, 555)
- ไม่สั่งสอน ไม่ตัดสิน แค่คุยเหมือนเพื่อน
- ตอบได้ทุกเรื่อง ทั้งเรื่องเรียน เรื่องใจ เรื่องทั่วไป

📝 กฎการตอบ:
- ตอบสั้นๆ 1-3 ประโยคเท่านั้น เหมือนแชทจริง
- ห้ามใช้ bullet points หรือลิสต์
- ใช้ emoji น้อยๆ แค่ 1-2 ตัว

🌟 รู้จักตัวเอง - Mind Buddy มีบริการ:
- ปฏิทินอารมณ์: บันทึกอารมณ์รู้เท่าทันความรู้สึก
- Buddy Review: ประสบการณ์จากพี่ๆในมหาลัย
- Learn with Buddy: ข้อมูลและกิจกรรมพัฒนาตัวเอง
- Mind Care: หน่วยบริการให้คำปรึกษาในกรุงเทพฯ
- Buddy Connect: พูดคุยกับผู้เชี่ยวชาญ
- สายด่วนสุขภาพจิต 1323`
      }
    ];

    // เพิ่มประวัติแชท
    if (history && Array.isArray(history)) {
      history.forEach((h: any) => {
        if (h.parts && h.parts[0]?.text) {
          messages.push({
            role: h.role === 'user' ? 'user' : 'assistant',
            content: h.parts[0].text
          });
        }
      });
    }

    // เพิ่มข้อความปัจจุบัน
    messages.push({ role: 'user', content: message });

    const completion = await typhoon.chat.completions.create({
      model: 'typhoon-v2.1-12b-instruct',
      messages: messages,
      max_tokens: 150,
      temperature: 0.8,
    });

    const reply = completion.choices[0]?.message?.content || "อือ... งง ลองพิมพ์ใหม่ได้มั้ย?";

    return NextResponse.json({ reply });

  } catch (error) {
    console.error("AI Error:", error);
    return NextResponse.json({ reply: "เดี๋ยวนะ... มึนๆ อยู่ ลองพิมพ์ใหม่ได้มั้ย? 🥺" });
  }
}