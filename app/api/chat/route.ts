import { NextResponse } from 'next/server';
import { GoogleGenerativeAI } from "@google/generative-ai";

// เรียกใช้ API Key (เดี๋ยวไปเอามาจาก .env)
const genAI = new GoogleGenerativeAI(process.env.GEMINI_API_KEY || "");

export async function POST(req: Request) {
  try {
    const { message, history } = await req.json();

    // เลือกโมเดล Gemini
    const model = genAI.getGenerativeModel({ model: "gemini-pro" });

    // คำสั่งลับ (Prompt) บอกให้ AI รู้ตัวว่าเป็นใคร
    const systemPrompt = `
      คุณคือ "MindBuddy" เพื่อน AI ที่อบอุ่น ใจดี และรับฟังเก่ง
      หน้าที่ของคุณคือ: ให้กำลังใจ รับฟังความทุกข์ และแนะนำวิธีแก้ปัญหาเบื้องต้นสั้นๆ
      - ห้ามตอบยาวเกิน 3 ประโยค
      - ใช้ภาษาที่เป็นกันเอง เหมือนคุยกับเพื่อนสนิท (ใช้ "เรา", "เธอ", "ตัวเอง")
      - ถ้าเขารู้สึกแย่ ให้แสดงความเห็นใจก่อนเสมอ
    `;

    // ส่งข้อความไปถาม AI
    const chat = model.startChat({
      history: [
        { role: "user", parts: [{ text: systemPrompt }] }, // สั่งบุคลิก
        { role: "model", parts: [{ text: "ได้เลย! เราพร้อมรับฟังนะ มีอะไรระบายมาได้เลย ❤️" }] },
        ...history // ประวัติการคุยเก่าๆ
      ],
    });

    const result = await chat.sendMessage(message);
    const response = result.response.text();

    return NextResponse.json({ reply: response });

  } catch (error) {
    console.error("AI Error:", error);
    return NextResponse.json({ reply: "ขอโทษนะ ตอนนี้เรามึนๆ นิดหน่อย ลองพิมพ์ใหม่ได้มั้ย? 🥺" });
  }
}